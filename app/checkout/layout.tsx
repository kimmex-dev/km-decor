export const metadata = {
  title: "Checkout",
  description: "Complete your KM Decor order — confirm delivery details and submit your purchase.",
};

export default function CheckoutLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
